extends layout.pug
include utils.pug

block content
    +errorSummary(errors)
    form(action=`/park/edit/${park.id}`, method="post")
        include park-form-fields.pug
        div
            button(type="submit") Update Park
            a(href=`/park/${park.id}`) Cancel
